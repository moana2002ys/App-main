kind = "web"
previewPath = "/"
title = "조각조각"
version = "1.0.0"
id = "artifacts/jogak"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 21025

[services.development]
run = "pnpm --filter @workspace/jogak run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/jogak", "run", "build" ]
publicDir = "artifacts/jogak/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "21025"
BASE_PATH = "/"
